<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 地図タイル取得のネットワーク状態判定用 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Android 11+ のパッケージ可視性制限対応。QR決済・ウォレットアプリ起動(JudgmentScreen)で
         getLaunchIntentForPackage() が正しく検出できるよう、対象アプリを明示する。
         未宣言だとインストール済みでも null が返り Play Store フォールバックに落ちる。 -->
    <!-- payment_methods.json の qr_payments[].app_packages と対で管理する(宣言が無いと Android 11+ で
         起動 Intent が取れず Play ストア送りになる)。リモート JSON でパッケージを追加してもここは
         アプリ更新でしか増やせない点に注意 -->
    <queries>
        <package android:name="jp.ne.paypay.android.app" />
        <package android:name="jp.auone.wallet" />
        <package android:name="com.nttdocomo.keitai.payment" />
        <package android:name="jp.co.rakuten.pay" />
        <!-- AEON Pay は単独アプリと iAEON の2本立て -->
        <package android:name="jp.co.aeon.credit.android.wallet" />
        <package android:name="jp.co.aeonst.app.myaeon" />
        <!-- メルペイ(メルカリアプリ内) -->
        <package android:name="com.kouzoh.mercari" />
        <!-- ウォレット(Google Pay)。domain の WALLET_APP_PACKAGE と対で管理 -->
        <package android:name="com.google.android.apps.walletnfcrel" />
    </queries>

    <application
        android:name=".PoikatsuApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Poikatsu">

        <!-- Google Maps SDK の APIキー。値は build.gradle.kts の manifestPlaceholders 経由で
             local.properties の MAPS_API_KEY を差し込む(リポジトリにはコミットしない)。 -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Poikatsu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>